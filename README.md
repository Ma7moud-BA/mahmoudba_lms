# Prisma Commands

- ` npx prisma generate`
  this command generates the tables inside the prisma client to sue in the app, make sure that the server is off

- `npx prisma db push`
  sync the data base with the prisma client.

- `npx prisma migrate reset`
  reset the data base data
